/* eslint import/no-extraneous-dependencies: off, @typescript-eslint/no-var-requires: off */
const { preprocess } = require('@pyoner/svelte-ts-preprocess');

module.exports = {
    preprocess: preprocess(),
};
